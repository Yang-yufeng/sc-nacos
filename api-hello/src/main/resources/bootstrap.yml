# 应用名称
spring:
  application:
    name: api-hello
  profiles:
    active: test
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yaml


